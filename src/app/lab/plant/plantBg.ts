/* Default backdrop for the plant canvas. Kept in its own r3f-free module so
   the homepage grid's server-rendered data can import it — see
   src/components/ThreeCanvas/canvasBg.ts for the full rationale. */
export const PLANT_BG = "#CFBD9F";
